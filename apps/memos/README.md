# Memos

Self-hosted, open-source (MIT) lightweight note-taking/memory app.

- **Image:** `ghcr.io/usememos/memos:v0.30.0`
- **Web UI:** port `${MEMOS_WEB_PORT}` (5230)
- **Data:** `${MEMOS_MOUNT_DIR}` (`/var/opt/memos`, sqlite)
