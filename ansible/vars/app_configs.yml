primary_owner: mrishab

app_configs:
  adguardhome:
    ports:
      - { port: 3000, proto: any }
      - { port: 53, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/adguard/work" }
      - { path: "{{ host_data_dir }}/adguard/conf" }
  affine:
    ports:
      - { port: 3010, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/affine/db", mode: "0700" }
      - { path: "{{ host_data_dir }}/affine/app/uploads" }
      - { path: "{{ host_data_dir }}/affine/app/config" }
  airflow:
    ports:
      - { port: 2364, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/airflow/app/plugins",
          owner: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/airflow/app/dags",
          owner: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/airflow/app/logs",
          owner: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/airflow/db",
          owner: "{{ primary_owner }}",
          mode: "0700",
        }
  bentopdf:
    ports:
      - { port: 8085, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/bentopdf/config",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/bentopdf/data",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  calibre:
    ports:
      - { port: 8080, proto: any }
      - { port: 8081, proto: any }
      - { port: 8083, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/calibre/calibre-db/books",
          owner: "201",
          group: "201",
        }
      - {
          path: "{{ host_data_dir }}/calibre/calibre-web/config",
          owner: "201",
          group: "201",
        }
  cadvisor:
    ports:
      - { port: 8383, proto: any }
    restart_on_deploy: true
    dirs: []
  cloudflared:
    ports: []
    restart_on_deploy: true
    sysctl:
      - { name: "net.core.rmem_max", value: "7500000" }
      - { name: "net.core.wmem_max", value: "7500000" }
    dirs: []
  codeserver:
    ports:
      - { port: 2342, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/codeserver/config",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  ddns:
    ports: []
    restart_on_deploy: true
    dirs: []
  deluge:
    ports:
      - { port: 8112, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/mediaserver/downloads",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/deluge/config",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  dockovpn:
    ports:
      - { port: 8386, proto: any }
      - { port: 1194, proto: udp }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/dockovpn",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  duplicati:
    ports:
      - { port: 8200, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/duplicati/config",
          owner: "2234",
          group: "2234",
          mode: "0700",
        }
  elk:
    ports:
      - { port: 9200, proto: any }
      - { port: 9300, proto: any }
      - { port: 5601, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/elk/elasticsearch/data" }
      - { path: "{{ host_data_dir }}/elk/kibana/data" }
      - { path: "{{ host_data_dir }}/elk/logstash/data" }
  epg:
    ports:
      - { port: 30000, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/epg/public" }
  freshrss:
    ports:
      - { port: 4523, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/freshrss/app" }
      - { path: "{{ host_data_dir }}/freshrss/db", mode: "0700" }
  ghost:
    ports:
      - { port: 2368, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/ghost/app",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - { path: "{{ host_data_dir }}/ghost/db" }
  grafana:
    ports:
      - { port: 3003, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/grafana", owner: "472", group: "472" }
  hermes:
    ports:
      - { port: 8642, proto: any }
      - { port: 9119, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/hermes",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
          mode: "0700",
        }
  homarr:
    ports:
      - { port: 7575, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/homarr/appdata",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  immich:
    ports:
      - { port: 2283, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/immich/app/library" }
      - { path: "{{ host_data_dir }}/immich/db/postgres", mode: "0700" }
      - { path: "{{ host_data_dir }}/immich/ml-cache" }
  jellyfin:
    ports:
      - { port: 8096, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/jellyfin/config" }
      - { path: "{{ host_data_dir }}/mediaserver/jellyfin/cache" }
  jenkins:
    ports:
      - { port: 8888, proto: any }
      - { port: 50000, proto: any }
      - { port: 10023, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/jenkins",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  jupyter:
    ports:
      - { port: 2705, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/jupyter/home/jovyan",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  lazylibrarian:
    ports:
      - { port: 5299, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/lazylibrarian/config" }
  lb:
    ports: []
    restart_on_deploy: true
    dirs: []
  mediaserver:
    ports:
      - { port: 8989, proto: any }
      - { port: 7878, proto: any }
      - { port: 8686, proto: any }
      - { port: 6767, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/mediaserver/downloads",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/music",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/movies",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/tv",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/courses",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/sonarr/config",
          owner: "201",
          group: "201",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/radarr/config",
          owner: "201",
          group: "201",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/lidarr/config",
          owner: "201",
          group: "201",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/bazarr/config",
          owner: "70",
          group: "{{ primary_owner }}",
        }
  n8n:
    ports:
      - { port: 5678, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/n8n/app" }
      - { path: "{{ host_data_dir }}/n8n/db", mode: "0700" }
  nextcloud:
    ports:
      - { port: 8765, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/nextcloud/db",
          owner: "70",
          group: "{{ primary_owner }}",
          mode: "0700",
        }
      - {
          path: "{{ host_data_dir }}/nextcloud/html",
          owner: "www-data",
          group: "www-data",
        }
      - {
          path: "{{ host_data_dir }}/nextcloud/redis",
          owner: "999",
          group: "999",
        }
  nexus:
    ports:
      - { port: 4634, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/nexus/nexus",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  metabase:
    ports:
      - { port: 8093, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/metabase/app" }
      - { path: "{{ host_data_dir }}/metabase/db", mode: "0700" }
  nzbget:
    ports:
      - { port: 6789, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/nzbget/config" }
  ollama:
    ports:
      - { port: 11434, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/ollama" }
  ombi:
    ports:
      - { port: 3579, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/ombi/config" }
  open-webui:
    ports:
      - { port: 11435, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/open-webui" }
  pgadmin:
    ports:
      - { port: 3487, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/pgadmin",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
          mode: "0750",
        }
      - {
          path: "{{ host_data_dir }}/postgresdb",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  plex:
    ports:
      - { port: 32400, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/mediaserver/plex/config",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/plex/transcode",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/music",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/movies",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/tv",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/mediaserver/courses",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  postiz:
    ports:
      - { port: 5000, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/postiz/app/config",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/postiz/app/uploads",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/postiz/db/data",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/postiz/redis/data",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  prometheus:
    ports:
      - { port: 9090, proto: any }
      - { port: 9093, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/prometheus" }
      - { path: "{{ host_data_dir }}/alertmanager" }
  prowlarr:
    ports:
      - { port: 9696, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/prowlarr/config" }
  registry:
    ports:
      - { port: 5000, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/registry" }
  reverse-proxy:
    ports:
      - { port: 80, proto: any }
      - { port: 443, proto: any }
      - { port: 9113, proto: any }
    restart_on_deploy: false
    dirs:
      - { path: "{{ host_data_dir }}/reverse-proxy/certbot" }
      - { path: "{{ host_data_dir }}/reverse-proxy/certificates" }
  sftpgo:
    ports:
      - { port: 9393, proto: any }
      - { port: 2022, proto: any }
      - { port: 2121, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/sftpgo/srv/sftpgo",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/sftpgo/var/lib/sftpgo",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  sonarqube:
    ports:
      - { port: 3124, proto: any }
    restart_on_deploy: true
    sysctl:
      - { name: "vm.max_map_count", value: "262144" }
    dirs:
      - {
          path: "{{ host_data_dir }}/sonarqube/db",
          owner: "70",
          group: "{{ primary_owner }}",
          mode: "0700",
        }
      - {
          path: "{{ host_data_dir }}/sonarqube/app/data",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/sonarqube/app/extensions",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/sonarqube/app/logs",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  stirling-pdf:
    ports:
      - { port: 8086, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/stirling-pdf/configs",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/stirling-pdf/logs",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/stirling-pdf/tessdata",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
      - {
          path: "{{ host_data_dir }}/stirling-pdf/pipeline",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  tautulli:
    ports:
      - { port: 8181, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/tautulli/config" }
  threadfin:
    ports:
      - { port: 34400, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/mediaserver/threadfin/config" }
      - { path: "{{ host_data_dir }}/mediaserver/threadfin/temp" }
  upsnap:
    ports:
      - { port: 8090, proto: any }
    restart_on_deploy: true
    dirs:
      - {
          path: "{{ host_data_dir }}/upsnap/data",
          owner: "{{ primary_owner }}",
          group: "{{ primary_owner }}",
        }
  wikijs:
    ports:
      - { port: 3123, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/wikijs/db", mode: "0700" }
  vikunja:
    ports:
      - { port: 3456, proto: any }
    restart_on_deploy: true
    dirs:
      - { path: "{{ host_data_dir }}/vikunja/db", owner: "{{ primary_owner }}", group: "{{ primary_owner }}" }
      - { path: "{{ host_data_dir }}/vikunja/files", owner: "{{ primary_owner }}", group: "{{ primary_owner }}" }
