version: '3'

services:
  nexus:
    hostname: nexus
    image: sonatype/nexus3
    restart: unless-stopped
    ports:
      - ${NEXUS_WEB_PORT}:8081
    volumes:
      - ${NEXUS_MOUNT_DIR}:/nexus-data
